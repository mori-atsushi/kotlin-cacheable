dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
    versionCatalogs {
        create("libs") {
            from(files("../gradle/libs.versions.toml"))
        }
    }
}

rootProject.name = "build-logic"
include(":chacheable-gradle-plugin")
project(":chacheable-gradle-plugin").projectDir = File("../cacheable-gradle-plugin")
